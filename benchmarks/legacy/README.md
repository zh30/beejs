# Legacy Benchmarks

This directory collects historical benchmark sources, benchmark helper scripts, compiled benchmark binaries, and saved result files that previously lived in the repository root.

Current benchmark work should prefer the existing `benches/` and `benchmarks/` structure. Keep generated reports under `benchmarks/reports/`.
