# Distribution Artifacts

This directory contains release archives and packaged release snapshots that were previously stored in the repository root.

Do not place generated build output here unless the task explicitly requires preserving a release artifact.
