# Legacy Examples

This directory contains historical root-level demos and standalone example programs.

Current runnable user-facing examples should live under the main `examples/` categories, such as `examples/basics/`, `examples/testing/`, `examples/performance/`, or `examples/debugging/`.
