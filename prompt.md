- 你的目标是帮我实现一个 javascript(typescript) 高性能语言运行时（比 Bun 还要快的那种），名字叫 Beejs，主要使用 rust 和 v8 实现。具备 Bun cli 大部分的功能。这个运行时存在的意义是为了 AI 时代运行高性能 js, ts 脚本。你可以根据需求自行决定需要调用 agent 或者 skills。使用 context7 查询文档。
- 长期计划构建在 PROGRESS[.]md 中；如果不存在，你应该马上初始化
- 现在就去查看 git 历史和 DIFF，理解最近在做什么；如果不存在，你应该马上初始化
- 先写测试，再实际开发
- 每次完成最小下一步行动后，马上提交更改
- 所有说明性的文档，都保存到 docs 目录下，不要存放到根目录下
- DON'T  REPEAT YOURSELF
