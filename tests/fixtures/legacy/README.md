# Legacy Test Fixtures

This directory contains historical fixture directories that previously lived in the repository root.

Use focused fixture directories near the tests that need them for new test data.
