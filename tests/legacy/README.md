# Legacy Root Tests

This directory contains standalone test files, shell checks, Cargo test manifests, and compiled test binaries that previously lived in the repository root.

Current Rust integration tests should live directly under `tests/` using Cargo's normal integration test layout.
